# Secure File System

## Dependency
Run the command below to build python environment.
```
pip install cryptography
```

## Initialize
Run the command below to initialize password management system.
```
cd password
python json_config.py
```

## How to test
Run the command in `scripts.sh` to test.
